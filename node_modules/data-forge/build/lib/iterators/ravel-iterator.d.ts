export declare class RavelIterator<T> implements Iterator<T> {
    iterators: Iterator<T>[];
    iteratorIndex: number;
    constructor(iterables: Iterable<T>[]);
    next(): IteratorResult<T>;
}
