export declare class TileIterable<T> implements Iterable<T> {
    iterable: Iterable<T>;
    count: number;
    constructor(iterable: Iterable<T>, count: number);
    [Symbol.iterator](): Iterator<any>;
}
